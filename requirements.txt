# Q-DECEM CO2 forecasting framework - pinned dependencies (spec 3.8).
# Generated from a verified working environment (Python 3.10). Versions are
# pinned exactly (not >=) so a fresh install reproduces the same behaviour;
# bump deliberately and re-run the full test suite + pipeline, not silently.

# Core
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3

# Machine learning
scikit-learn==1.7.2
lightgbm==4.6.0
catboost==1.2.8
xgboost==3.1.3

# Deep learning (LSTM)
torch==2.9.1

# Visualization
matplotlib==3.10.8
seaborn==0.13.2

# SHAP explanations
shap==0.49.1

# Statistics (VIF, ARIMA baseline, DM-test helpers)
statsmodels==0.14.6

# Data handling
openpyxl==3.1.5
pyarrow==23.0.0

# Grid data ingestion (GB Carbon Intensity API, section 6)
requests==2.32.5

# Configuration
PyYAML==6.0.3

# Testing
pytest==9.0.2
pytest-cov==7.0.0

# Utilities
tqdm==4.67.1
joblib==1.5.3
